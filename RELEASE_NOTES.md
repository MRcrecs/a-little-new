# Release v0.1.0

## Что вошло

- база сайтов с категориями
- поиск и фильтрация по сайтам
- индивидуальные и общие `path`
- импорт и экспорт JSON
- импорт `path` между сайтами
- массовое открытие общих `path` по категории
- проверка HTTP-статусов
- раскрываемые детали по сайтам для общего `path`
- сборка в `.exe` и релизная папка

## Основные файлы релиза

- `url-auto-opener.exe`
- `README.md`

## Примечания

- локальная база сайтов хранится отдельно в `sites.json`
- `sites.json` не входит в репозиторий и релизный архив по умолчанию
